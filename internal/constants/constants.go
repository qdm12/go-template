// Package constants contain constants shared by packages in the program.
package constants

import (
	"github.com/qdm12/go-template/internal/models"
)

const (
	AccountUser  models.AccountType = "user"
	AccountAdmin models.AccountType = "admin"
)
